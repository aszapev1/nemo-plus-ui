export type IconProps = {
	variant: string;
	width: number;
	height: number;
	turn: 'up' | 'right' | 'down' | 'left';
};


export type IconPropsContainer = {
	variant: string;
	width: number;
	height: number;
};
